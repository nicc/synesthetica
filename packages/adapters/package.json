{
  "name": "@synesthetica/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Input adapters for Synesthetica (MIDI, Audio)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@spotify/basic-pitch": "^1.0.1",
    "@synesthetica/contracts": "*",
    "@tensorflow/tfjs": "^3.21.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
