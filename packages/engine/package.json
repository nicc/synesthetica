{
  "name": "@synesthetica/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Core engine for Synesthetica (pipeline, stabilizers, rulesets, grammars)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@synesthetica/contracts": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
