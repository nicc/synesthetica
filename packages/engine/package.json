{
  "name": "@synesthetica/engine",
  "version": "0.1.0",
  "private": true,
  "license": "BUSL-1.1",
  "description": "Core engine for Synesthetica (pipeline, stabilizers, rulesets, lenses)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@synesthetica/contracts": "*",
    "three": "^0.182.0",
    "tonal": "^6.4.3"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
